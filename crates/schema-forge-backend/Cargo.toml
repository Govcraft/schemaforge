[package]
name = "schema-forge-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
schema-forge-core = { path = "../schema-forge-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
