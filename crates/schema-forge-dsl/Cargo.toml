[package]
name = "schema-forge-dsl"
version = "0.10.0"
edition = "2021"

[dependencies]
schema-forge-core = { path = "../schema-forge-core" }
schema-forge-cel = { version = "0.6.0", path = "../schema-forge-cel" }
logos = "0.15"
tracing = "0.1"

[dev-dependencies]
proptest = "1"
